#ifndef SAMOA_PARTITION_ROUTER_HPP
#define SAMOA_PARTITION_ROUTER_HPP

namespace samoa {
    
class partition_router {
public:
    
    partition_router(io_service &);
    
    route_request(const request::ptr_t &);
    
    
    
};

};

#endif

